# Frontend (Vite) - set in Vercel environment variables
VITE_SERVER_URL=https://chat-app-server.fly.dev

# Backend (server.js) - set via `fly secrets set` or fly.toml [env]
CORS_ORIGIN=https://your-app.vercel.app
